// const request = require('supertest');
// const startServer = require('../app');

describe('Testes para a aplicação Koa', () => {
    // let server;

    // beforeAll(() => {
    //     server = startServer();
    // });

    // afterAll((done) => {
    //     server.close(done);
    // });

    it('Deve retornar Hello, Koa! na rota principal', async () => {
        // const response = await request(server).get('/');
        // expect(response.status).toBe(200);
        // expect(response.text).toContain('Heartbeat Simulation');
    });

});
